# Logs
logs/

# macOS
.DS_Store

# Runtime
.daily-research*.lock

# Personal data (use *.example.* as templates)
config.toml
past_topics.json
past_topics.json.bak

# MCP config (project-scoped MCP servers, if any)
.mcp.json

# Concept cluster graph backup (graph.jsonld itself is tracked)
graph.jsonld.bak

# Personal launchd plist (use com.example.daily-research.plist as template)
com.daily-research*.plist
com.*.daily-research.plist
!com.example.daily-research.plist

# Local Claude Code settings
.claude/settings.json
.claude/settings.local.json

# Analysis reports
.reports/

# Internal planning notes (not for public repo)
.notes/

# Personal operational logs (postmortems, session transcripts, evaluations)
docs/progress/
docs/archive/

# Evaluation scores (personal data, use scores.example.jsonl as schema reference)
evals/scores.jsonl
