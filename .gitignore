# Logs
logs/

# macOS
.DS_Store

# Runtime
.daily-research*.lock

# Personal data (use *.example.* as templates)
config.toml
config.toml.bak
past_topics.json
past_topics.json.bak
metrics.jsonl
metrics.jsonl.bak*

# MCP config (project-scoped MCP servers, if any)
.mcp.json

# Concept cluster graph backup (graph.jsonld itself is tracked)
graph.jsonld.bak

# Personal launchd plist (use com.example.daily-research.plist as template)
com.daily-research*.plist
com.*.daily-research.plist
!com.example.daily-research.plist

# Local Claude Code settings
.claude/settings.json
.claude/settings.local.json

# Analysis reports
.reports/

# Internal planning notes (not for public repo)
.notes/

# Personal operational logs (postmortems, session transcripts)
docs/progress/
docs/archive/

# Python test environment
.venv/
.pytest_cache/
__pycache__/
.coverage

# Per-line research state (watched-sources / playbook / last-seen; operational, private)
state/

# 旧 concept-graph 同期 dir (ADR-0008 で廃止)。ローカルに残骸が残りうるので ignore は維持
.repo-graphs/
